{
	"name": "led",
	"version": "0.0.1",
	"description": "L-Ed, a lightweight & efficient 2D level editor",
	"main": "app.html",

	"window": {
		"title": "LEd",
		"width": 800,
		"height": 600,
		"show": true
	},

	"chromium-args": "--enable-spell-checking",
	"js-flags": "--expose-gc",

	"build": {
		"nwVersion": "0.46.4"
	},

	"scripts": {
		"dist": "build --tasks win-x64 --mirror https://dl.nwjs.io/ ."
	},

	"dependencies": {},
	"devDependencies": {
		"nwjs-builder-phoenix": "^1.15.0"
	}
}
